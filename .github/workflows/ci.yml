# Runs format/lint/typecheck/build across the monorepo (currently apps/reference
# plus the shared packages/*) on every push/PR to main.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format:
    name: Format Check
    uses: ./.github/workflows/format.yml
    secrets: inherit

  lint:
    name: Lint
    uses: ./.github/workflows/lint.yml
    secrets: inherit

  typecheck:
    name: Typecheck
    uses: ./.github/workflows/typecheck.yml
    secrets: inherit

  build:
    name: Build
    needs: [format, lint, typecheck]
    uses: ./.github/workflows/build.yml
    secrets: inherit
